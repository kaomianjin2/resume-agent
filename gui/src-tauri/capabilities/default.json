{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-shell",
  "description": "Main window access for desktop shell controls and native file selection.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
